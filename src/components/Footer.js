import React from "react";
import "../styles/Footer.css";

const styles = {
    footerStyle: {
        background: "black"
    }
};

const Footer = props => (
    <footer className="footer">
    <h1 style={styles.footerStyle}>TESTING</h1>
    </footer>

);

export default Footer;